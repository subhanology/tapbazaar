// Placeholder until the Products feature is copied into the project —
// this route/link already works end-to-end, it just has nothing to fetch yet.
const MyListings = () => (
  <main className="mx-auto max-w-3xl px-6 py-16 text-center">
    <h1 className="mb-2 text-[22px] font-semibold tracking-heading text-ink">My Listings</h1>
    <p className="text-sm text-ink-secondary">
      This will show the products you've listed once the Products feature is wired up. Nothing to
      do here yet — just confirming the route and nav link work.
    </p>
  </main>
);

export default MyListings;